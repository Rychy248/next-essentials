/** @type {import('next').NextConfig} */

const nextConfig = {
  /** 
   * ## this is only availble at the latest canary version of next. Now 2024 09 22 still is not
      experimental:{
        ppr:'incremental',
      }
   */
};

export default nextConfig;
